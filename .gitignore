# local agent settings (permissions are machine-specific)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# scratch / temp build files (the authoring recipe uses /tmp, not the repo)
/tmp/
*_old.html
*.bak
gallery-registry-draft.json

# browser-verification scratch (Playwright MCP screenshots/snapshots) & Python cache
.playwright-mcp/
__pycache__/

# node (only used transiently by validate.py via `node --check`)
node_modules/
.grepai/
